import styled from 'styled-components';

export const TextInput = styled.input`
  font-size: 1rem;

  border: none;
  padding: 0.6rem;
  margin: 0;

  border-radius: 10px;
  box-shadow: 0px 5px 9px -3px #bcbcbc;

  :focus {
    outline: none;
  }
`;
